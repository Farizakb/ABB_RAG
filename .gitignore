# --- Planning and process. Internal to Fariz, not part of the ABB deliverable. ---
# Untracked, not deleted: the fresh implementation session still reads these off disk.
archive/
docs/superpowers/
INDEX.md
SPEC.md
SPEC_v*.md
RECON.md
CUT_LIST.md
*.docx

# NOTE: docs/ itself is deliberately NOT ignored. docs/adr/ and
# docs/error-analysis.md are deliverables and must ship.

# Subagent-driven-development scratch: ledger, briefs, reports, review packages.
.superpowers/

# --- Secrets ---
.env
.env.*
!.env.example

# --- Crawl cache: regenerable, large, and ABB's content. Never committed. ---
data/
!data/corpus_sample.json
!data/corpus_three_page.json

# NOTE: backend/scraper/tests/fixtures/raw/*.html are the gate-captured pages every
# extraction test asserts against, tracked despite living under a test tree.
# data/corpus_sample.json is what lets a reviewer run the app without crawling
# ABB's production site.

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Node ---
node_modules/
dist/
.vite/
*.tsbuildinfo

# --- Editors and OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
