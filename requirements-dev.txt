# Pinned for scripts/verify_gate.py.
httpx==0.28.1
openai==3.13.0

# Test and lint toolchain (make test / make lint / CI).
pytest==9.1.1
ruff==0.14.14
mypy==2.3.1
pre-commit==4.6.2

# packages/contracts models (rag/chat shared schema).
pydantic==2.13.5

# services/rag/conftest.py builds and migrates a `*_test` database directly
# from the host to run ingest tests -- needs psycopg on the host even though
# it's already pinned in services/rag/requirements.txt for the container.
# Pins match that file exactly.
psycopg[binary]==3.3.5
psycopg-pool==3.3.1

# packages/scraper sitemap filter.
# httpx is already pinned above; reused here for fetch_sitemap's client param.
# selectolax: fast (Lexbor-backed) HTML/XML parser used to parse sitemap.xml's <url>/
# <loc>/<lastmod> nodes in fetch_sitemap.
selectolax==0.4.11

# packages/scraper polite fetcher.
# protego: stdlib urllib.robotparser is first-match-wins (1994 draft), which on ABB's
# actual robots.txt allows /cgi-bin/ despite its Disallow; protego implements RFC 9309
# most-specific-match.
protego==0.6.2
