# services/chat runtime dependencies. Every version pinned exactly (Invariant:
# reproducible builds). Where a package is also pinned in services/rag's own
# requirements.txt, the pin here matches it -- one version per package.
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
psycopg[binary]==3.3.5
psycopg-pool==3.3.1
httpx==0.28.1
# Per-session and per-IP rate limit on POST /api/v1/questions -- "ten lines
# with slowapi" instead of hand-rolling a token bucket.
slowapi==0.1.10
