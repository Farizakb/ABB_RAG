[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "shared"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic==2.13.5", "psycopg[binary]==3.3.5", "psycopg-pool==3.3.1"]

[tool.setuptools]
packages = ["shared"]
