[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "abb-scraper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx==0.28.1", "selectolax==0.4.11", "protego==0.6.2"]

[tool.hatch.build.targets.wheel]
packages = ["abb_scraper"]
